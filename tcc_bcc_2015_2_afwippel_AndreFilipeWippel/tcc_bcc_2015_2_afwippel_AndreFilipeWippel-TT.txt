ATENÇÃO … existe duas versões de código … uma para roda na Web e outra para dispositivos móveis. No caso:
O código da versão ios e Android estão na pasta tagarelaMobile.
E o código da aplicação web está ficou na pasta tagarelaWeb.

----- UTILIZA«√O DO PHONEGAP -----

Para instalar o phonegap CLI, siga estes passos:
- Baixe e instale o Node.js. ApÛs a instalaÁ„o, vocÍ deve ser capaz de executar o comando 'npm' em seu prompt de comando (ou terminal).
- Instale o phonegap atravÈs do comando 'npm install -g phonegap'

Para criar um projeto PhoneGap (apenas um HeloWorld):
- phonegap create hello com.example.hello HelloWorld

Para buildar o projeto criado:
- cd hello
- phonegap build ios
- phonegap build android

Para testar a aplicaÁ„o em um emulador:
- phonegap emulate ios
- phonegap emulate android

Para executar a aplicaÁ„o em um dispositivo concetado na porta usb:
- phonegap run ios
- phonegap run android